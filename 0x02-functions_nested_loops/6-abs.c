#include "main.h"
/**
 * _abs - prints absolute value
 * @n: the parameter of the function
 * Return: returns as described below
 */

int _abs(int n)
{
if (n > 0)
{
return (n);
}
else if (n < 0)
{
return (-n);
}
return (0);
}
