#include <stdio.h>

/**
 * main - this program prints the alphabets is lower case
 * Return: returns 0 if successful
 */

int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (ch != 'q' && ch != 'e')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
