#include <stdio.h>

/**
 * main - print all single digit nos of base 10 staring from 0
 * Return: returns 0 if successful
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
